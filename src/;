#include "lametop.h"

void print_table() {
	
}

void print_rows(int rows) {
	int i = 0;
	while() {
		
	}
}

int *get_max_column_width(int *cur_width, snapshot *file) {
	pid_values *process;
	while(file->next) {
		process = file->process;
	}
}
